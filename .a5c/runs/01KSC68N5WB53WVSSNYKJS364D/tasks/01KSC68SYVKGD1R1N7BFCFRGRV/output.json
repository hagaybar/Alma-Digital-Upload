{
  "findings": [
    {"file": "alma_digital_upload.py", "line": "399-404", "category": "boto3-loggers", "what": "boto3.resource() called with explicit aws_access_key_id / aws_secret_access_key", "whySensitive": "boto3/botocore loggers can emit request signing detail at DEBUG; repo sets root logger to DEBUG so a stray boto3 DEBUG could expose credential-adjacent data", "severity": "possible-leak", "proposedFix": "change-log-level: pin logging.getLogger('botocore')/('boto3')/('urllib3') to WARNING in setup_logging()"},
    {"file": "alma_digital_upload.py", "line": "60-66", "category": "debug-file-handler", "what": "Root logger DEBUG; FileHandler DEBUG -> timestamped log file; no repo-side scrubbing filter", "whySensitive": "DEBUG file logs aggregate detailed records; repo relies entirely on almaapitk 0.4.5 redaction with no own filter", "severity": "theoretical", "proposedFix": "change-log-level: set FileHandler to INFO and/or add a repo-side logging.Filter scrubbing config dicts and exception text (overlaps file-writes:65)"}
  ],
  "logConfig": {"level": "root DEBUG; file_handler DEBUG; console_handler INFO", "destinations": ["{output}/alma_upload_{ts}.log", "stdout INFO"], "filters": "none repo-side; relies on almaapitk 0.4.5 TextFormatter for Authorization redaction", "redactionHolds": false},
  "historyNotes": [
    "No actual secret values ever committed; only os.getenv() references across 7 commits",
    "*.log gitignored; no log files in git history",
    "historical_logs/ untracked: only metadata (MMS IDs, library/set codes), no keys",
    "commit e894719 upgraded almaapitk to >=0.4.5 for toolkit-level Authorization redaction"
  ],
  "notes": "Section 4 CLEAN: no .env; config.example.json placeholders only; all creds from env vars (ALMA_*_API_KEY, AWS_ACCESS_KEY, AWS_SECRET, *_BUCKET_NAME). Section 5: DEBUG file logging with no repo-side scrub filter -> redactionHolds=false (repo-side); boto3 keys passed explicitly. Section 7 CLEAN: no committed secrets, history rewrite not needed."
}
